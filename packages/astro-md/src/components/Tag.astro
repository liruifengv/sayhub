---
const className = `bg-sky-500 rounded-md text-white inline-block mr-4 mb-4 py-0.5 px-2`

const { tagName } = Astro.props

const basename = import.meta.env.BASE_URL
---

<div class={className}>
  <a href={`${basename}tag/${tagName}`}>
    <slot />
  </a>
</div>
